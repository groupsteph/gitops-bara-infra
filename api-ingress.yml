---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: bara-api
  annotations:
    kubernetes.io/ingress.class: gce
spec:
  rules:
  - http:
      paths:
      - path: /*
        backend:
          serviceName: bara-api
          servicePort: 80